(ns cider-ci.executors.shared
  )

(def allowed-keys [
                   :description
                   :name
                   :enabled
                   :upload_tree_attachments
                   :upload_trial_attachments
                   ])
